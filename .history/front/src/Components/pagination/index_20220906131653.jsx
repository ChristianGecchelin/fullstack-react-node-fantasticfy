import React from "react";
const Pagination = (props) => {
  const { list, setList } = props;

  return (
    <>
      <ul>
        <li>numeros</li>
      </ul>
    </>
  );
};

export default Pagination;
